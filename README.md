# bluegreen-nginx-image

```bash
> docker run -it --rm -e COLOR=green -p 8080:80 amasucci/bluegreen
```

```bash
> docker run -it --rm -e COLOR=blue -p 8080:80 amasucci/bluegreen
```

[http://localhost:8080](http://localhost:8080)
